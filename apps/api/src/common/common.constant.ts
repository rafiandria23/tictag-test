export enum PaginationPage {
  Min = 1,
}

export enum PaginationSize {
  Min = 1,
  Max = 500,
  Default = 10,
}

export enum SortDirection {
  Asc = 'asc',
  Desc = 'desc',
}

export enum DocsTag {
  Auth = 'Auth',
  Dashboard = 'Dashboard',
  User = 'User',
  Product = 'Product',
}
